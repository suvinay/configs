[core]
	editor = vim
	autocrlf = false
	safecrlf = true
[user]
	name = Suvinay Subramanian
	email = suvinay@csail.mit.edu
[alias]
    co = checkout
    ci = commit
    st = status
    br = branch
    hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
    type = cat-file -t
    dump = cat-file -p
    graph = log --oneline --decorate --graph --all
    who = shortlog -s --
    local = log origin/master..HEAD
[color]
	ui = true
